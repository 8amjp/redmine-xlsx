extends layout

block title
  title Import/Export | Redmine Xio

block content
  .container
    .row
      .col-md-6.py-3
        h1 Import/Export Ticket
        p.lead チケットに対して処理を行います。
      .col-md-6.py-3
        .form-group
          label(for='currentdata') 現在のデータ:
          textarea#currentdata.form-control(rows='4', readonly='')
    .row
      .col-md-6.py-3
        .card.border-success
          .card-body
            h4.card-title.text-success Import
            p.card-text 指定したエクセル形式の帳票をインポートします。
            input#file-input.form-control-file(type='file')
            .text-right.my-3
              button#import.btn.btn-success(type='button', onclick='importExcel()') Import
            textarea#result.form-control(rows='3')
      .col-md-6.py-3
        .card.border-success
          .card-body
            h4.card-title.text-success Export
            p.card-text エクセル形式の帳票をダウンロードします。
            .text-right.my-3
              button#export.btn.btn-success(type='button', onclick='exportExcel()') Export

block additional-script
  script(src='/javascripts/core.js', id='core', data-issue='#{issue}', data-table='#{table}')